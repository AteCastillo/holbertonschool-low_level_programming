#include "holberton.h"

/**
 * int mul - multiplies two integers.
 * @a: int1 received
 * @b: int2 received
 * Return: Always 0.
 */

int mul(int a, int b)

{
	int res;

	res = (a * b);
	{
		_putchar (res);
	}
	return (0);
}
