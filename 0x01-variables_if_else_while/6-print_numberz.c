#include <stdio.h>
/**
*main - Entry point
*Return: Always 0 (Success)
 */

int main(void)
{
        int nu;

        for (nu = '0' ; nu <= '9' ; nu++)
        {
                putchar(nu);
        }
        putchar('\n');
        return (0);
}
