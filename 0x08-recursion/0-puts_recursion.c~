#include "holberton.h"

/**
 * _puts_recursion - check the code for Holberton School students.
 * @s: pointer
 * Return: Always 0.
 */

void _puts_recursion(char *s)

{
	int n;

	if (s[n] = '\0')

	{
		_putchar('\n');
	}

	_putchar(s[n]);
	_putchar(_puts_recursion (s[n+1]));

}
