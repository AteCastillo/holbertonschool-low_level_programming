#include "holberton.h"
/**
 * copy_toanother_file - check the code for Holberton School students.
 * @argc: pointer to name of the file
 * @argv: pointer to pointer of the file
 * Return: Always 0.
 */
int main(int argc, char argv[])
{
	int fd, buffer;

	buffer = malloc(sizeof(char) * 1024);
	
	if( 


}
