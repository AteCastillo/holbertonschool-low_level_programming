#include "0x1E-search_algos.h"

/**
 * binary_search - search algorithms in a binary way
 * array: pointer to the first element of the array
 * size: size of the array
 * value: value to search for
 * return: the index
 */

int binary_search(int *array, size_t size, int value)
{
    if (array == NULL)
        return (-1);

    if (size == 0)
        return (1);
    else
    {   
        newsize = (size / 2)
        if (array[index] == value)
        return (index);
    }
    return (-1);
}